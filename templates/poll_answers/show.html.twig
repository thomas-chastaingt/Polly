{% extends 'base.html.twig' %}

{% block title %}PollAnswers{% endblock %}

{% block body %}
    <h1>PollAnswers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poll_answer.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('poll_answers_index') }}">back to list</a>

    <a href="{{ path('poll_answers_edit', {'id': poll_answer.id}) }}">edit</a>

    {{ include('poll_answers/_delete_form.html.twig') }}
{% endblock %}
